# AGENTS.md

> 本文件是给 AI Agent（也包括未来的自己）阅读的项目说明文档。它解释了**这个项目是什么、为什么这样做、怎么协作**，而不是每一步的具体命令。
>
> 具体步骤命令请配合 [`LEARNING.md`](./LEARNING.md)（学习路线）和 [`docs/`](./docs)（各阶段图文教程）一起使用。

---

## 1. 项目背景（Why）

这是一个**学习型项目**，不是生产级项目。它的核心目标不是“做出一个完美的个人网站”，而是：

> **借助 AI Agent 的辅助，完整走一遍「写代码 → 部署 → 更新 → 再部署」的纯前端个人网站全生命周期**，理解现代前端项目在 GitHub + Cloudflare 上的协作与发布流程。

之所以选择这个切入点，是因为它覆盖了现代 Web 开发几乎所有重要环节，但门槛足够低——只要会写 HTML，就能开始。

### 1.1 仓库基本信息

| 项 | 值 |
| --- | --- |
| 仓库名 | `mengyu-lab` |
| GitHub | <https://github.com/LOMOGMY/mengyu-lab> |
| 所有者 | 个人学习账号 `LOMOGMY` |
| 当前状态 | 空仓库（仅有 `README.md`、`.gitignore`、`AGENTS.md`） |
| 分支策略 | `main` 为发布分支；功能分支命名 `feat/<name>`、`fix/<name>` |

### 1.2 平台选型：为什么是 Cloudflare？

经过调研，最终选定的部署平台是 **Cloudflare Pages**（后续会用到 Pages Functions + D1 数据库）：

- **免费额度充足**：Pages 静态托管、Functions 请求次数、D1 数据库读写都提供远高于学习场景的免费配额，对零成本学习非常友好。
- **学习价值高**：覆盖了静态部署、Serverless、边缘计算、托管数据库等现代 Web 概念，是一个“麻雀虽小五脏俱全”的练习场。
- **与 GitHub 集成好**：直接连接 GitHub 仓库，push 即部署，pr 即预览，是学习 CI/CD 最直观的方式。
- **后续可扩展**：从纯静态 → 加 Functions → 加 D1，是一条平滑的渐进式学习路径。

> 备选方案（Vercel / Netlify / GitHub Pages）也很好，但本项目为了把“部署 + Serverless + 数据库”一条线打通，固定使用 Cloudflare，避免在学习阶段切换平台带来的认知负担。

---

## 2. 项目目标（What）

### 2.1 最终形态（远期愿景）

一个托管在 Cloudflare Pages 上的**个人网站**，具备：

1. **静态内容**：个人介绍、项目展示、博客文章等纯前端页面。
2. **交互能力**：通过 Cloudflare Pages Functions 提供 API。
3. **数据存储**：使用 Cloudflare D1（SQLite）保存访客留言、表单提交等内容。
4. **完整 DevOps**：GitHub → Cloudflare 自动部署，PR Preview 可用。

### 2.2 学习阶段（当前路线）

按“从易到难、每个阶段都能独立跑通并部署”的原则，分成 5 个阶段：

| 阶段 | 主题 | 产物 | 状态 |
| --- | --- | --- | --- |
| **P0** | 项目初始化 + 工具链搭建 | `AGENTS.md` / `LEARNING.md` / 目录结构 / 本地预览脚本 | ✅ 已完成 |
| **P1** | 第一个静态页面 | 一个 `index.html`，部署到 Cloudflare Pages | ✅ 已完成 |
| **P2** | 内容扩展 | 多页面（项目详情 + 博客） + 卡片化样式 | ✅ 已完成 |
| **P3** | 加入交互 + 数据库 | 联系表单 + Pages Functions + D1 | 🚧 当前阶段 |
| **P4**（可选） | 进阶 | Turnstile 反垃圾 / 分页 / Access 管理后台 | ⏳ |
| **P5**（可选） | 进阶 | 自定义域名 / 分析 / 博客系统 / RSS | ⏳ |

每个阶段结束后，都会**真实部署一次**，确保你看到“代码 → 线上”的完整链路。

---

## 3. 协作约定（How we work）

### 3.1 谁做什么

| 角色 | 职责 |
| --- | --- |
| **你（学习者）** | 决定产品方向、提供内容（文字、图片、个人信息）；在 Cloudflare 控制台、GitHub 网页端完成账号、绑定、部署等需要人工确认的操作（**这是学习的关键，AI 不能替你完成这些步骤**，但 AI 会给出图文并茂的操作指南）。 |
| **AI Agent** | 负责：① 写代码（HTML/CSS/JS/Functions/SQL）；② 提供 Cloudflare/GitHub 操作指引（步骤、要办的勾选、常见坑）；③ 解释“为什么这么做”；④ 帮你 review 代码、找 bug、优化结构。 |

> 一句话：**AI 写代码 + 你点鼠标**。所有“云平台上点哪里、填什么、勾什么”的事情，AI 会一步一步告诉你，但需要你自己去操作台完成。

### 3.2 代码相关约定

- **技术栈**：纯 HTML / CSS / 原生 JavaScript（前期）；后期 Functions 部分使用 **TypeScript + Hono**（Cloudflare 官方推荐，轻量、类型友好）。
- **不使用框架**：P1–P2 不引入 React/Vue，先建立对 Web 基础的理解；P3 起允许引入构建工具（Vite）。
- **目录结构**（P1 起会逐步演进）：

  ```text
  mengyu-lab/
  ├── AGENTS.md            ← 本文件（项目元信息）
  ├── LEARNING.md          ← 学习路线总览
  ├── README.md            ← 项目对外说明
  ├── docs/                ← 各阶段图文教程（人读）
  │   ├── p1-first-page.md
  │   ├── p2-multi-page.md
  │   └── ...
  ├── public/              ← 静态资源（图片、字体）
  ├── src/                 ← 源代码（HTML/CSS/JS）
  │   ├── index.html
  │   ├── styles/
  │   └── scripts/
  ├── functions/           ← Cloudflare Pages Functions（从 P3 开始）
  │   └── api/
  └── wrangler.toml        ← Cloudflare 本地开发 + 部署配置（从 P3 开始）
  ```

- **提交规范**：使用 Conventional Commits（如 `feat: add about page`、`docs: p1 deployment guide`）。
- **每次“完成一个可部署的小阶段”就 commit + push 一次**，触发 Cloudflare 自动部署。

### 3.3 沟通约定

- AI 在执行**任何会修改你 GitHub 仓库或 Cloudflare 账号的操作前**，必须先告诉你“你需要去 X 处手动做 Y”。
- AI 在写代码前会先解释“为什么要这么写”，不直接甩大段代码。
- 每个阶段结束后，会给你一个**自检清单**（例如：“打开 <https://mengyu-lab.pages.dev> 能看到你的网站”），确认通过再进入下一阶段。

---

## 4. 学习路径（Learning Path）

每一阶段都遵循同一个循环：

```text
写代码 → 本地预览 → 提交 → 推送到 GitHub → Cloudflare 自动部署 → 验证线上效果 → 进入下一阶段
```

这个循环会重复很多次，**这就是我们要学习的核心 DevOps 流程**。

### 4.1 当前阶段：P0 — 项目初始化

**目标**：

1. 建立项目文档骨架（你正在看的这份文件）。
2. 建立后续教程的目录结构。
3. 决定本地预览方案（先从最简单的 `python -m http.server` 或 `npx serve` 开始）。

**不做什么**：还不到写第一个 HTML 的时候——先把“地基”打好。

### 4.2 下一阶段预告：P1 — 第一个静态页面

将做的事情：

1. 写一个最简单的 `index.html`：包含你的名字、一句话介绍、一个标题。
2. 在本地预览（看到效果）。
3. `git add / commit / push` 到 GitHub。
4. **你去 Cloudflare 控制台**：连接 GitHub 仓库 → 创建 Pages 项目 → 自动部署。
5. 拿到你的第一个线上 URL：`https://mengyu-lab.pages.dev`（或类似）。

P1 的详细图文教程会写在 `docs/p1-first-page.md`。

---

## 5. 非目标（Non-goals）

为了把精力集中在学习上，明确**不做**的事：

- ❌ 不做账号系统、不做登录鉴权（学习阶段用 Cloudflare Access 即可，避免过度工程）。
- ❌ 不做 SEO/性能优化到极致（P5 之后再回头看）。
- ❌ 不引入重型前端框架（React/Vue/Next.js 等）——这不是项目的目的。
- ❌ 不使用自定义域名（等你熟悉整个流程后再加，避免混淆主线任务）。

---

## 6. 常见问题

**Q：为什么不让 AI 帮我点 Cloudflare 控制台？**
A：Cloudflare 的登录、DNS、计费绑定都涉及你的账号安全和付费（即使是免费额度）。更重要的是，**这是你学习的核心环节**——亲手操作才能记住流程。AI 会给你逐步指引，包括截图位置描述和“要勾/要填/要点”的明确动作。

**Q：如果我中途卡住了怎么办？**
A：随时停下来告诉我具体卡点（贴报错信息、截图），我会帮你分析。**不要**为了推进而跳过验证步骤——没在浏览器里看到效果，等于没完成。

**Q：可以改技术栈吗？**
A：可以，但建议先按既定路线走完一遍。如果你学完 P2 后特别想换成 React，那是另一个学习项目了，可以另开仓库。

---

## 7. 变更日志

| 日期 | 变更 | 作者 |
| --- | --- | --- |
| 2026-08-26 | 初始化项目文档骨架，写入背景、目标、协作约定 | AI Agent（与你协作） |
| 2026-08-26 | P1：第一个静态页面 + 部署到 Cloudflare Pages | AI Agent + 你 |
| 2026-08-26 | P2：多页面架构（项目详情 + 博客）+ 自动部署演示 | AI Agent + 你 |
| 2026-08-26 | P3：联系表单 + Pages Functions + D1 数据库 | AI Agent + 你 |

---

> **下一步建议**：确认这份 `AGENTS.md` 没问题后，告诉我“我准备好了，开始 P1”。我会创建 `LEARNING.md` 和 `docs/` 目录结构，并开始 P1 的第一个静态页面。